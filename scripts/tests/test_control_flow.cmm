main() {
    bits32 count = 0;
    bits32 sum = 0;

loop_start:
    count = count + 1;
    sum = sum + count;

    if (count < 10) {
        goto loop_start;
    } else {
        foreign "C" printf(sum);
    }

    return sum;
}
